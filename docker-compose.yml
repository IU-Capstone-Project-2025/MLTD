services:
  backend:
    build: ./backend
    volumes:
      - log_storage:/api/logs
    ports:
      - 8080:8000
    networks:
      - mltd
  frontend:
    build: ./frontend
    ports:
      - 3000:3000
    networks:
      - mltd

volumes:
  log_storage:

networks:
  mltd: